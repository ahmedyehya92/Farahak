<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_review"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.kywline.far7a.activities.ReviewActivity">

<RelativeLayout
    android:padding="16dp"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <RatingBar
        android:id="@+id/rating_bar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="70dp"

        />
    <TextView
        android:id="@+id/tx_clickToReview"
        android:text="اضغط لتقوم بالتقييم"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/rating_bar"
        android:layout_alignStart="@id/rating_bar"
        android:layout_alignLeft="@id/rating_bar"
        android:layout_marginTop="5dp"
        android:textColorHighlight="@color/bg_login"/>

    <EditText
        android:id="@+id/et_review"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"

        android:padding="10dp"
        android:layout_marginBottom="10dp"
        android:hint="قم بكتابة تعليق"
        android:minLines="2"
        android:maxLines="3"
        android:scrollbars="vertical"
        android:inputType="textMultiLine"
        android:layout_below="@id/tx_clickToReview"
        android:layout_marginTop="30dp"
        android:background="@color/listView_background"
        />

    <Button
        android:id="@+id/submit_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/et_review"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="30dp"
        android:background="@drawable/button_review_shape"
        android:text="إضافة التقييم"
        android:textColor="@color/white"

        />
</RelativeLayout>
</ScrollView>
